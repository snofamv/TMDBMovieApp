// Archivo actual: MenuScreen
import {View, Text, Pressable, StyleSheet} from 'react-native';

const MenuScreen = () => {
  return (
    <View>
      <Text>MenuScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default MenuScreen;
